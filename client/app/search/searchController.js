/*
 angular
        .module('event')
        .controller('searchController', searchController);

    searchController.$inject = ['$reactive', '$scope'];
    function searchController($reactive, $scope) {
        $reactive(this).attach($scope);
        let vm = this;
        //let r = vm.recherche;
        vm.res = vm.recherche.split(" ");
        console.log(vm.res);
    }
*/
